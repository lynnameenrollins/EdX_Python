# -*- coding: utf-8 -*-
"""
Spyder Editor

This is a temporary script file.
"""

y[1]=1;
x[1]=1;
deltaX=0.1
index=1;
steps = (2.5-1.0)/0.1;


for (index = 1:steps):
    y[index + 1]=y[index] + deltaX*(x[index] + 3*y[index]/x[index];
    x[index +1] = x[index] + deltaX;
end                                
                                     